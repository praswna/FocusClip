<Window x:Class="FocusClip.Views.PromptEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="프롬프트 편집"
        Width="540" Height="460"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBrush}"
        Foreground="White"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Bg3Brush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="팝업 그룹 — ▾에서 고르거나 새 이름 입력(새 팝업 생성)"
                   Foreground="{StaticResource GrayBrush}" FontSize="11" Margin="2,0,0,4"/>
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="GroupNameBox" Grid.Column="0"
                     Background="{StaticResource Bg2Brush}" Foreground="White"
                     BorderBrush="{StaticResource LineBrush}" Padding="6" FontSize="14"/>
            <Button x:Name="GroupPick" Grid.Column="1" Content="▾" Click="GroupPick_Click"
                    ToolTip="기존 그룹에서 선택" MinWidth="34"/>
        </Grid>

        <TextBlock Grid.Row="2" Text="제목" Foreground="{StaticResource GrayBrush}"
                   FontSize="11" Margin="2,0,0,4"/>
        <TextBox x:Name="TitleBox" Grid.Row="3"
                 Background="{StaticResource Bg2Brush}" Foreground="White"
                 BorderBrush="{StaticResource LineBrush}" Padding="6" FontSize="14"
                 Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="본문 (휠로 글자 크기 조절)" Foreground="{StaticResource GrayBrush}"
                   FontSize="11" Margin="2,0,0,4"/>
        <TextBox x:Name="BodyBox" Grid.Row="5"
                 AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 Background="{StaticResource Bg2Brush}" Foreground="White"
                 BorderBrush="{StaticResource LineBrush}" Padding="6" FontSize="14"
                 PreviewMouseWheel="Body_PreviewMouseWheel"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="저장" Click="Save_Click" Background="{StaticResource AccentBrush}"/>
            <Button Content="취소" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
