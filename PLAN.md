# FocusClip 개발 계획서

## 목표
FocusClip — 런처(FocusManager) + 클립보드(Clipboard-Manager)를 통합한 .NET 8 WPF 트레이 앱.
원본 FM/CM 동작을 충실히 재현하면서 기능을 점진적으로 개선한다.

---

## 우선순위 백로그

### ✅ 완료
- D1: 이미지 드래그 시 느림 해결 (파일 드롭 우선, 비트맵 폴백)
- D2: 텍스트 클립 드래그앤드롭 지원
- D3: 경로 팝업 드래그앤드롭 지원 (텍스트 전용)
- D4: 드래그 한자 깨짐 수정 (StringFormat 제거, SetText만 사용)

### 🔵 제안
- ✅ P001: 드래그 실패 시 토스트 알림 ("드롭 미지원 앱", Esc 취소는 제외)
- P002: 클립 팝업에서 텍스트 카드 우클릭 → "드래그용 임시 파일 생성" (txt 파일로 탐색기 드롭 지원)
- P003: 경로 팝업 — 로컬 경로 드래그 시 파일 드롭도 선택적으로 제공 (토글 설정)
- ✅ P004: 클립보드 항목 영구 저장 (clips.json, 텍스트/이미지/경로 모두)
- P005: 클립 팝업 검색 필드 (카드 수 많을 때 텍스트 빠른 검색)
- P006: 카드에 드래그 핸들(⠿) 아이콘 표시 — 드래그 가능함을 시각적으로 안내
- P007: 사용 빈도 기반 클립 자동 정렬 (자주 쓰는 항목 상단 고정 옵션)
- P008: 이미지 클립 카드에 썸네일 미리보기 (현재는 텍스트 레이블만 표시)
- P009: 클립 항목 최근 사용 시각 표시 (카드 hover 툴팁)
- P010: 클립 히스토리 JSON 내보내기/가져오기 (백업·기기 이전)
- P011: 트레이 우클릭 메뉴에 "클립 히스토리 전체 지우기" 항목 추가
- ✅ P012: 핀 고정 클립 항목 재시작 후에도 유지 (clips.json에 Pinned 포함)
- P013: 런처 도크 앱 아이콘 자동 갱신 (앱 업데이트로 아이콘 변경 시 캐시 재추출)
- P014: 클립 저장 용량 설정 (현재 고정 30개, 설정 창에서 변경 가능하게)
- P015: clips.json 손상 시 자동 백업 복구 (저장 전 .bak 파일 유지)
- P016: 클립 팝업 핀 상태를 별도 색상으로 강조 (현재 핀/비핀 카드 구분이 약함)
- P017: 앱 종료 없이 설정에서 클립 히스토리 최대 개수 즉시 반영
- P018: 이미지 카드 로딩 중 스켈레톤 플레이스홀더 표시 (비동기 로드 중 빈 카드 방지)
- ✅ P019: 용량 초과 시 밀려난 비핀 항목의 본문 파일까지 삭제 (media 폴더 무한 증가 방지)

---

## 변경 이력
| 날짜 | 내용 |
|------|------|
| 2026-06-15 | D1~D4 완료: 드래그앤드롭 텍스트/경로 지원 + 한자 깨짐 수정 |
| 2026-06-16 | P001/P004/P012 완료: 드래그 실패 토스트 + 클립 영구 저장 + 핀 유지 |
| 2026-06-16 | DoSave 파일 쓰기 백그라운드 처리 (UI 스레드 블로킹 위험 제거) |
| 2026-06-16 | Dispose 종료 시 동기 저장으로 분리 (Task.Run 미완료로 인한 히스토리 유실 버그 수정) |
| 2026-06-16 | LoadHistory 이미지 비동기 로드 (UI 스레드 블로킹 제거 — PNG 30개 수 초 멈춤 방지) |
| 2026-06-16 | 텍스트/경로도 파일(.txt)로 저장, clips.json은 메타데이터만 (본문 파일 경로 참조) |
| 2026-06-16 | 저장 위치 전부 %LOCALAPPDATA%\FocusClip 통일 (OneDrive 하이드레이션 지연 제거) + Roaming 자동 마이그레이션 |
| 2026-06-16 | 클립 카드 "저장 위치 열기"(📂) 버튼 + 트레이 "저장 폴더 열기" 추가 |
| 2026-06-16 | 빌드 산출물 publish\ 한 곳으로 통일 (csproj에 배포옵션 통합, build.bat이 bin/obj/pdb 정리) |
| 2026-06-16 | (철회) 별도 STA 스레드 OLE 드래그 — 워커 스레드가 마우스를 못 잡아 드래그 실패 → UI 스레드 파일드롭으로 복귀 |
| 2026-06-16 | 마이그레이션 버그 수정: 새 폴더가 이미 있으면 건너뛰어 config·icons 누락 → 파일 단위 복사로 변경 |
| 2026-06-16 | 드래그 단순화: 임시 복사본 제거, 저장된 PNG를 그대로 파일 드롭(Copy 전용). 우리 쪽 복사·인코딩 0 |
| 2026-06-16 | P019: 용량 초과 eviction 시 본문 파일도 삭제 (media 폴더 무한 증가 방지) |
